{
  "name": "env-builder",
  "version": "1.1.0-beta.0",
  "description": "A lightweight env builder",
  "keywords": [
    "typescript"
  ],
  "scripts": {
    "dev": "vitest",
    "build": "tsc",
    "ci": "npm run build && npm run check-format && npm run test",
    "local-release": "changeset version && changeset publish",
    "prepublishOnly": "npm run ci",
    "test": "vitest run",
    "format": "prettier --write .",
    "check-format": "prettier --check ."
  },
  "homepage": "https://github.com/em-blaze/env-builder",
  "bugs": {
    "url": "https://github.com/em-blaze/env-builder/issues"
  },
  "author": "Emblaze",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/em-blaze/env-builder.git"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.9.2",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "zod": "^4.0.15"
  }
}
